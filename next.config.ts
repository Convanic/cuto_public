import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // Für Docker standalone output (optional)
  // output: 'standalone',
  
  // Für Pfad-basiertes Routing (z.B. /cuto) - nur aktivieren wenn nötig
  // basePath: '/cuto',
  // assetPrefix: '/cuto',
};

export default nextConfig;
