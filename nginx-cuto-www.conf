# Nginx Konfiguration für www.cuto.ai (Production)
# Diese Konfiguration funktioniert parallel zu Apache

# WICHTIG: Prüfe vorher, ob Apache auf Port 80/443 läuft
# Falls ja, siehe Optionen unten

# ============================================
# OPTION 1: Nginx auf Port 80/443 (wenn Apache auf anderen Ports)
# ============================================

# HTTP Server (Port 80) - leitet zu HTTPS weiter
server {
    listen 80;
    listen [::]:80;
    server_name www.cuto.ai cuto.ai;

    # Weiterleitung zu HTTPS (nach SSL Setup)
    # Vor SSL: Kommentar entfernen und auf Port 3000 weiterleiten
    # return 301 https://$server_name$request_uri;
    
    # Vor SSL Setup: Direkt auf Port 3000 weiterleiten
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS Server (Port 443) - nach SSL Setup aktivieren
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.cuto.ai cuto.ai;

    ssl_certificate /etc/letsencrypt/live/cuto.ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cuto.ai/privkey.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Security Headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Statische Assets cachen
    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }
}

# ============================================
# OPTION 2: Nginx auf Port 8080/8443 (wenn Apache auf 80/443)
# ============================================
# Falls Apache auf Port 80/443 läuft, kommentiere Option 1 aus
# und nutze diese Konfiguration:

# server {
#     listen 8080;
#     listen [::]:8080;
#     server_name www.cuto.ai cuto.ai;
#     
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
# 
# server {
#     listen 8443 ssl http2;
#     listen [::]:8443 ssl http2;
#     server_name www.cuto.ai cuto.ai;
#     
#     ssl_certificate /etc/letsencrypt/live/cuto.ai/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cuto.ai/privkey.pem;
#     
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

